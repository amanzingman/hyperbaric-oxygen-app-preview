name: Deploy preview

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/configure-pages@v5
      - run: |
          python3 - <<'PY'
          from pathlib import Path
          import zipfile

          destination = Path("_site")
          destination.mkdir()
          with zipfile.ZipFile("高压氧治疗室APP-Web交付包.zip") as archive:
              for info in archive.infolist():
                  target = destination / info.filename.replace("\\", "/")
                  if info.is_dir():
                      target.mkdir(parents=True, exist_ok=True)
                  else:
                      target.parent.mkdir(parents=True, exist_ok=True)
                      with archive.open(info) as source, target.open("wb") as output:
                          output.write(source.read())
          index = destination / "index.html"
          contents = index.read_text()
          contents = contents.replace("<base href=\"/\">", "<base href=\"/hyperbaric-oxygen-app-preview/\">")
          contents = contents.replace("<title>肾内科随访助手</title>", "<title>高压氧治疗室</title>")
          index.write_text(contents)
          bootstrap = destination / "flutter_bootstrap.js"
          bootstrap_contents = bootstrap.read_text()
          bootstrap.write_text(bootstrap_contents.replace("{\"engineRevision\":", "{\"useLocalCanvasKit\":true,\"engineRevision\":"))
          PY
      - uses: actions/upload-pages-artifact@v4
        with:
          path: _site
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
